import DocsLayout from "../../../components/DocsLayout";

<DocsLayout>

### When do you need an API key?

You need an API key to live transcode .ts segments via http push.

You do not need to create an API key for the RTMP ingest workflow.

</DocsLayout>