import DocsLayout from "../../../components/DocsLayout";

<DocsLayout>

### How to authenticate with your API key

You need an API key to live transcode .ts segments via http push. Learn [how to create an API key](/docs/api_keys_create_an_api_key).

You must send this key in the Authorization header when making requests:

`Authorization: Bearer <api-key>`

</DocsLayout>