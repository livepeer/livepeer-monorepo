{
  "name": "@livepeer/monitor",
  "version": "0.0.1",
  "description": "Monitors Livepeer's infrastructure",
  "main": "dist/monitor.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "parcel build --target node --bundle-node-modules --no-minify --no-source-maps -o dist/index.js src/monitor.js && gcloud functions deploy livepeer-monitor-automatic --source dist --entry-point poll"
  },
  "author": "Livepeer",
  "license": "MIT",
  "dependencies": {
    "graphql": "^14.2.1",
    "isomorphic-fetch": "^2.2.1"
  },
  "browserslist": [
    "node 10"
  ],
  "devDependencies": {
    "esm": "^3.2.22",
    "parcel": "^1.12.3"
  }
}
